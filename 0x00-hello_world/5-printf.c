#include <stdio.h>
/**
 * main - Program that print a line using printf funtions
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
